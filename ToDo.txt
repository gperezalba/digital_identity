1. La librería blockchainWeb3 pasarla a un repo independiente e incluir el repo
al package.json ("git": "gitrepo"). (así se importarían con require creo)

2. contratos en 4.15 cambiar version truffle para solc 4.15 o cambiar pragma (?)
